name: "Continuous Deployment Printscript-UI-Dev"
on:
  push:
    branches:
      - dev
  workflow_dispatch: {}
jobs:
  call_workflow:
    permissions:
      contents: read
      packages: write
    uses: ElTaladro37/Actions/.github/workflows/docker-publish.yml@main
    with:
      service_name: "printscript-ui"
      branch_name: "dev"
      container_name: "printscript-ui"
    secrets: inherit